#pragma once
namespace LAB3 {
	enum class Keys {
		Exit = 48									// Key 0, Выход
		, PrintArray = 49							// Key 1, Распечатать массив
		, PrintHashTable = 50						// Key 2, Распечатать хеш-таблицу
		, ClearArraiAndHashTable = 51				// Key 3, Очистить массив и хеш таблицу
		, ResizemyType = 53							// Key 9, Изменить размер массива и пересоздать хеш-таблицу
		, 
	};

	enum class isVisibleClear {
		ON
		, OFF
	};
}




